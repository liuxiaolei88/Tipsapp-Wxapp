<!--pages/Q&A/Q&A.wxml-->

<view wx:for="{{showDateInfo}}" wx:for-index="idx">
  <van-card
  num=""
  price=""
  title= "{{showDateInfo[idx]['title']}}"
  desc="{{showDateInfo[idx]['dec']}}"
  thumb="{{showDateInfo[idx]['pic']}}"
/>
</view>






